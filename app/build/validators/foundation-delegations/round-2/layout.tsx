import { ReactNode } from 'react'

export const metadata = {
  title: 'Round 2 - Closed | Persistence Docs',
  description: 'Documentation for Persistence One ecosystem',
}

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
